{
  "_comment": "Copy to credentials.json and fill in real values ONLY for local, non-EAS-managed builds. credentials.json is gitignored and must never be committed. Prefer `eas credentials` instead of keeping a local copy at all — it stores the keystore on Expo's servers and removes the file you'd otherwise have to protect.",
  "android": {
    "keystore": {
      "keystorePath": "credentials/android/keystore.jks",
      "keystorePassword": "REPLACE_WITH_KEYSTORE_PASSWORD",
      "keyAlias": "REPLACE_WITH_KEY_ALIAS",
      "keyPassword": "REPLACE_WITH_KEY_PASSWORD"
    }
  }
}
